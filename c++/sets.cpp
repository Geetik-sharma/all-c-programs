// set is datastructure which store only unique elements
//store elements in sotred order
#include<iostream>
#include<set>
using namespace std;
int main(){

    //set
    // set<string>s;
    // s.insert("abc");//log(n)
    // s.insert("def");
    // s.insert("ghi");
    // auto it=s.find("abc");//return the iterator of the element
    // s.erase("abc");//it can take both iterator and value
    // for(string s1 :s){
    //     cout<<s1<<endl;
    // }
    // if(it!=s.end()){//log(n)
    //     cout<<(*it)<<endl;
    // }

    // unordered set(store in unsorted oreder)

    //multiset
    //multi set allow multiple value
    //mutliset.find(x) return the iterator of the ffirst element
    //deletion by iterator i  multi set will delete the value an iterator only 
    //deletion by value in multi set will delete all occurance of the value


}
